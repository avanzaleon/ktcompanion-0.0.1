(function(){
const KEY='kt_companion_matrices_pdf2';
function read(){try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch(e){return[]}}
function esc(s){return String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]))}
function arr(x){return Array.isArray(x)?x:[]}
function ops(x){return Object.entries(x||{}).map(([n,q])=>esc(n)+(q>1?' ×'+q:'')).join(', ')||'Ninguno'}
function weapons(x){return arr(x).map(w=>esc(w.name)+': '+arr(w.weapons).map(esc).join(', ')).join(' | ')||'Ninguna'}
function show(){const box=document.getElementById('savedList');if(!box)return;const a=read();if(!a.length){box.innerHTML='<div class="card">No hay matrices guardadas en este dispositivo.</div>'}else{box.innerHTML=a.map((s,i)=>{let turns='';for(let t=1;t<=4;t++){const f=s.turns?.friend?.[t]||{},r=s.turns?.enemy?.[t]||{};turns+='<div class="turn"><b>T'+t+'</b><br>🟢 Estrategia: '+esc(arr(f.strategy).join(', ')||'—')+'<br>🟢 Tiroteo: '+esc(arr(f.firefight).join(', ')||'—')+'<br>🟢 Equipo facción: '+esc(arr(f.faction).join(', ')||'—')+'<br>🟢 Equipo universal: '+esc(arr(f.universal).join(', ')||'—');if(s.enemy)turns+='<br>🔴 Estrategia: '+esc(arr(r.strategy).join(', ')||'—')+'<br>🔴 Tiroteo: '+esc(arr(r.firefight).join(', ')||'—')+'<br>🔴 Equipo facción: '+esc(arr(r.faction).join(', ')||'—')+'<br>🔴 Equipo universal: '+esc(arr(r.universal).join(', ')||'—');turns+='</div>'}return '<article class="card"><div class="saved-actions"><button onclick="window.exportPDF&&window.exportPDF('+i+')">📄 Exportar PDF</button><button class="danger" onclick="window.delSavedSafe('+i+')">🗑️ Borrar</button></div><h3>⚔️ '+esc(s.friend)+(s.enemy?' vs '+esc(s.enemy):'')+'</h3><div class="mut">🗺️ '+esc(s.map)+' · 🎯 '+esc(s.operation)+' · 🕒 '+esc(s.date)+'</div><p>🟢 <b>Operativos:</b> '+ops(s.counts?.friend)+'</p><p>🔴 <b>Rival:</b> '+ops(s.counts?.enemy)+'</p><details><summary>🔫 Armas</summary><p>🟢 '+weapons(s.weapons?.friend)+'</p><p>🔴 '+weapons(s.weapons?.enemy)+'</p></details><details><summary>📓 Registro T1-T4</summary>'+turns+'</details></article>'}).join('')}
const saved=document.getElementById('saved'),frame=document.getElementById('game');if(saved)saved.style.display='block';if(frame)frame.style.display='none';window.scrollTo(0,0)}
window.showSaved=show;window.delSavedSafe=function(i){const a=read();a.splice(i,1);localStorage.setItem(KEY,JSON.stringify(a));show()};
})();